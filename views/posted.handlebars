
{{#if loggedIn}}
<div class="columns is-centered">
    <div class="column is-half">
        <div class="box">
            <p><strong>{{post.title}}</strong></p>
            <p>{{post.content}}</p>
            <p>posted by:{{post.user.username}}</p>
        </div>
    </div>
</div>

<div class="columns is-centered">
    <div class="column is-half">
        {{#each post.comments}}
        <div class="box">
            <p>{{this.content}}</p>
        </div>
        {{each}}
    </div>
</div>

<div class="columns">
    <div class="columns is-half">
        <div class="box">
            <textarea
            id="comment-text"
            class="textarea"
            placeholder="comment">
            </textarea>
            <button
            class="button is-dark"
            id="comment-btn"
            data-value="{{post.id}}">Post Comment</button>
        </div>
    </div>
</div>
{{else}}
<a href="/">Login</a>
{{/if}}
<script src="../js/post.js"></script>
    {{!-- <div class="col-md-6 jumbotron" id='view-past-blogs'>
        <h2 class="text-center text-weight-bold" id="display-blog-titles">{{blogs.title}}</h2>
        <p>{{blogs.content}}</p>
        <h4 class="text-center text-weight-bold" id="display-userName">posted by {{post.user}}</h4>
    </div>
</div>

<div class="row">
    <div class="text-center text-weight-bold" id="display">
        <div class="col-md-6" id="display-comments">
            {{#each post.comment}}
            <div class="container-fluid">
                <p>{{this.post.content}}</p>
            </div>
            {{/each}}
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="container=fluid">
            <input type="text" class="form-control" aria-describedby="sizing-lg" id="text-comments"
                placeholder="Write comments here">
            <button class="bi bi-text-left" id="comment-submit">Submit Comment</button>
        </div>
    </div>
</div>
{{else}}
<a href="/signin">login</a>
{{/if}}
<script src="./index.js"></script> --}}